description "Sphinx Upstart Script"

start on startup
stop on shutdown

console output

respawn

script
    env RAILS_ENV=production
    exec sudo -u www-data -i /usr/local/bin/searchd --config <%= node[:site_app][:root] %>/config/production.sphinx.conf --nodetach
end script